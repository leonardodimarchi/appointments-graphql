{
  "name": "appointments-graphql",
  "version": "1.0.0",
  "description": "A simple API",
  "main": "index.js",
  "scripts": {
    "start:dev:simple": "ts-node-dev --respawn --transpile-only simple-server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "apollo-server": "^3.12.0",
    "graphql": "^16.6.0"
  }
}
